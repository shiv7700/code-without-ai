/**
 * Enter adds the item; every other key does not
 *
 * Topics: keyboard events · event.key · clearing a controlled input
 * Read:   https://react.dev/learn/responding-to-events
 * Read:   https://developer.mozilla.org/docs/Web/API/KeyboardEvent/key
 *
 * Rules:
 *  1. a text box labelled "New item" and a list under it
 *  2. Enter adds the trimmed text to the list and empties the box
 *  3. Enter on a box holding nothing but spaces adds nothing
 *  4. any other key adds nothing
 *
 * The box has to be emptied by the same thing that fills it, or it will not
 * empty at all. Note what goes into the list versus what you compared against
 * to decide whether there was anything worth adding.
 */
export default function QuickAdd() {
  return null
}
