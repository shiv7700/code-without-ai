/**
 * A checkbox carries its answer somewhere other than where you expect
 *
 * Topics: controlled input · checkbox · event target
 * Read:   https://react.dev/reference/react-dom/components/input
 * Read:   https://react.dev/learn/reacting-to-input-with-state
 *
 * Rules:
 *  1. a checkbox labelled "Email me updates", off to begin with
 *  2. clicking it turns it on, clicking again turns it back off
 *  3. a paragraph reads "On" or "Off" and always agrees with the box
 *
 * A text box hands you what was typed. A checkbox has nothing typed in it, so
 * the thing it hands you is a fixed string that never changes — and a string
 * that never changes cannot switch anything off.
 */
export default function NewsletterOptIn() {
  return null
}
