/**
 * Props in, JSX out: the smallest possible component
 *
 * Topics: props · JSX curly braces · default values
 * Read:   https://react.dev/learn/passing-props-to-a-component
 * Read:   https://react.dev/learn/javascript-in-jsx-with-curly-braces
 *
 * Rules:
 *  1. renders `Hello, {name}!` inside an <h1>
 *  2. no `name` given — greet "there" instead
 *  3. `excited` — the one "!" becomes "!!!"
 *
 * Everything a component receives arrives as one object. Curly braces are how
 * you get a value out of JS and into the markup — nothing more than that.
 */
export default function Greeting() {
  return null
}
