# Supabase → Project Settings → API. Copy this file to `.env` and fill both in.
# The publishable key is meant to ship in the browser — RLS is what keeps rows
# private, so it is not optional.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
