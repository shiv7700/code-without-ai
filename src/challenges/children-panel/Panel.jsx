/**
 * Children is just another prop
 *
 * Topics: children · composition · conditional rendering
 * Read:   https://react.dev/learn/passing-props-to-a-component#passing-jsx-as-children
 * Read:   https://react.dev/learn/your-ui-as-a-tree
 *
 * Rules:
 *  1. `title` in an <h3>
 *  2. everything nested inside the tag renders in a [data-testid="body"] below it
 *  3. whatever is passed comes through untouched — text, one element, or several
 *  4. nothing nested inside — no body element at all
 *
 * You never build children. You are handed them already built, and your only
 * job is to decide where in your markup they land.
 */
export default function Panel() {
  return null
}
